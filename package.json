{
    "name": "interview-prep-monorepo",
    "private": true,
    "workspaces": [
        "frontend",
        "backend"
    ],
    "scripts": {
        "dev": "concurrently -n BE,FE -c green,cyan \"yarn workspace backend dev\" \"yarn workspace frontend start\"",
        "build": "yarn workspace backend build && yarn workspace frontend build"
    },
    "devDependencies": {
        "concurrently": "^8.0.1"
    }
}
